# Archived snippets

Files moved out of `src/app` so routes stay focused. Restore by copying back into `src/app/...` (see comments inside each file).

- **`recording-background-page/`** — Full-screen ColorBends page previously at `src/app/test/page.tsx` (handy for screen recording backgrounds).
